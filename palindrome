# Palindrome Checker Program
# This script verifies whether the entered text is a palindrome.

value = input("Enter text or number: ").lower()

# Compare the string with its reversed version
if value == value[::-1]:
    print("Result: Palindrome detected")
else:
    print("Result: Not a palindrome")

